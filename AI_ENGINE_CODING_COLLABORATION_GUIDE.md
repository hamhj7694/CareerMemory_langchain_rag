# AI 엔진 코딩 협업 가이드

이 문서는 Career Memory의 AI 엔진을 Codex와 함께 개발할 때 지켜야 하는 작업 방식과 설명 기준을 정리한다.

다른 PC나 새로운 Codex 세션에서 작업하더라도 먼저 이 문서를 읽고 같은 방식으로 개발한다.

---

## 1. 개발 목표

Codex가 기능을 직접 구현하되, 사용자가 나중에 코드를 열어 다음 내용을 스스로 이해할 수 있어야 한다.

- 데이터가 어디에서 들어오는지
- 각 변수가 무엇을 의미하는지
- 함수가 어떤 순서로 실행되는지
- 프론트엔드와 Python 스키마가 어떻게 연결되는지
- AI 모델에 무엇을 전달하는지
- AI 결과를 어떻게 검증하고 반환하는지
- 코드가 정상적으로 동작하는지 어떻게 확인하는지

단순히 작동하는 코드를 만드는 것뿐만 아니라 사용자가 읽고 학습할 수 있는 코드를 만드는 것을 목표로 한다.

---

## 2. Codex의 기본 작업 방식

Codex는 다음 원칙에 따라 직접 코드를 작성한다.

1. 한 번에 하나의 작은 기능을 구현한다.
2. 작업 전에 관련 문서와 기존 코드를 확인한다.
3. 프론트엔드 요청과 Python 스키마를 먼저 대조한다.
4. 현재 프로젝트의 파일 구조와 코드 스타일을 유지한다.
5. 복잡한 축약 표현보다 처리 순서가 드러나는 코드를 사용한다.
6. 중요한 코드에는 역할과 이유를 설명하는 한국어 주석을 작성한다.
7. 기존 GPT 코드를 삭제하거나 덮어쓰지 않는다.
8. Gemini와 OpenAI를 설정으로 전환할 수 있는 구조를 유지한다.
9. 실제 API 키나 비밀값을 코드와 Git에 기록하지 않는다.
10. 구현이 끝나면 기능 범위에 맞는 테스트를 실행한다.
11. 작업 결과와 아직 구현하지 않은 범위를 명확히 구분해서 사용자에게 설명한다.

사용자가 명시적으로 설명이나 검토만 요청한 경우에는 코드를 수정하지 않는다.

---

## 3. 작업 전에 읽어야 하는 문서

AI 엔진 작업을 시작하기 전에 다음 문서를 우선순위대로 확인한다.

1. `Data_Flow_Summary.md`
   - 사용자의 서비스 이용 흐름
   - AI 기능별 전체 파이프라인

2. `AI_Engine/AI_ENGINE_DEVELOPMENT_GUIDE.md`
   - AI 엔진 개발 원칙
   - 엔진별 책임과 구현 기준

3. `작업 가이드.md`
   - 스키마 이후의 구현 순서
   - 단계별 작업 범위

4. `AI_Engine/AI_FRONTEND_CONTRACT_MAPPING.md`
   - 프론트엔드와 AI 스키마의 필드 대응
   - intent와 AI mode 변환
   - AI 결과와 공개 API 응답 변환

5. `docs/API_CONTRACT_WORKSPACE.md`
   - API 공통 규칙
   - 오류, 재시도, 요청 ID, 시간 형식

6. `docs/v2/V2_API_CONTRACT.md`
   - 실제 V2 API 주소와 요청·응답 계약
   - 대화, 메시지, 첨부, Proposal 계약

그다음 작업 대상에 따라 아래 코드를 확인한다.

- 프론트엔드 화면: `src/pages/`, `src/features/`
- 프론트엔드 API: `src/api/`
- AI 스키마: `AI_Engine/schemas/`
- AI 엔진: `AI_Engine/chatbot_ai.py`, `experience_ai.py`, `job_analysis_ai.py`
- 모델 Provider: `AI_Engine/llm_provider.py`
- 테스트: `AI_Engine/tests/`
- 코드 구성 방식 확인: `AI_Engine/레퍼런스/`

레퍼런스 폴더는 코드의 학습 난이도, 구성 순서, 라이브러리 사용 방식을 확인하는 용도로 사용한다. 현재 서비스의 스키마와 API 계약이 레퍼런스보다 우선한다.

---

## 4. 코드 작성 스타일

### 4.1 파일 상단 설명

각 파일의 첫 부분에는 해당 파일이 담당하는 역할을 짧게 설명한다.

```python
"""프론트엔드의 채팅 요청을 받아 대화형 AI 엔진으로 전달하는 API 라우터."""
```

### 4.2 번호가 있는 기능 구역

코드가 실행 흐름대로 읽히도록 주요 구역을 번호로 나눈다.

```python
# 1. 외부 라이브러리

# 2. 요청 데이터 스키마

# 3. 프론트엔드 intent 변환

# 4. 채팅 메시지 API
```

번호는 장식이 아니라 파일을 위에서 아래로 읽을 수 있도록 실행 순서와 책임을 구분하는 데 사용한다.

### 4.3 함수 설명

함수 위의 주석이나 docstring에는 가능하면 다음 내용을 담는다.

- 입력으로 무엇을 받는지
- 내부에서 무엇을 처리하는지
- 무엇을 반환하는지
- 변환이나 검증이 필요한 이유

```python
def convert_intent_to_mode(intent: str) -> ChatMode:
    """프론트엔드 intent를 AI 엔진이 사용하는 ChatMode로 변환한다."""
```

### 4.4 변수 설명

이름만으로 역할을 파악하기 어려운 변수는 생성 위치에서 설명한다.

```python
# 사용자 메시지를 먼저 저장한 뒤 생성되는 고유 ID다.
# AI 실행 결과와 원본 사용자 입력을 연결할 때 사용한다.
message_id = create_message_id()
```

모든 줄에 주석을 붙이지 않는다. 데이터의 출처, 변환 이유, 안전 규칙처럼 사용자가 흐름을 이해하는 데 필요한 부분에 작성한다.

### 4.5 피해야 하는 코드

학습을 어렵게 만드는 다음 형태는 특별한 이유가 없다면 피한다.

- 여러 작업을 한 줄에 합친 코드
- 의미가 불분명한 한 글자 변수
- 지나치게 중첩된 삼항 연산자
- 설명 없는 동적 속성 접근
- 같은 데이터 변환이 여러 파일에 중복된 코드
- 실패 이유를 숨기는 광범위한 예외 처리

---

## 5. 주석 작성 규칙

주석은 코드가 실제로 담당하는 역할과 그 이유를 설명한다.

좋은 예:

```python
# URL에서 받은 대화 ID를 AI 요청에도 사용해
# 같은 대화의 이전 메시지를 같은 메모리 공간에 연결한다.
conversation_id = path_conversation_id
```

좋은 예:

```python
# 프론트엔드는 experience라는 이름을 사용하지만,
# AI 엔진 내부에서는 experience_extraction 모드로 구분한다.
```

주석과 문서에는 구현 출처나 작성 주체를 불필요하게 강조하는 표현을 사용하지 않는다.

주석의 주제는 코드의 출처가 아니라 현재 서비스에서의 역할이어야 한다.

---

## 6. 프론트엔드와 연결할 때의 확인 순서

스키마만 보고 API를 구현하지 않는다. 다음 순서로 실제 프론트엔드 코드와 대조한다.

1. 사용자가 상호작용하는 컴포넌트를 찾는다.
2. 버튼이나 폼이 호출하는 함수를 찾는다.
3. API 함수에 전달하는 객체를 확인한다.
4. Mock과 실제 HTTP 요청이 분리되는 위치를 확인한다.
5. 공개 API 계약의 요청 형식과 비교한다.
6. Python Pydantic 스키마와 필드를 대조한다.
7. 서로 다른 변수명이나 enum을 Adapter에서 변환한다.
8. 응답이 화면 모델로 변환되는 과정을 확인한다.

예시:

```text
ChatComposer
  → ChatPage.submit()
  → 프론트엔드 메시지 API
  → FastAPI Router
  → ChatRequest
  → ChatbotAI
  → Gemini 또는 OpenAI
  → ChatResponse
  → 공개 Message 응답
  → MessageThread
```

프론트엔드 필드와 AI 내부 필드가 다르면 한쪽을 임의로 변경하지 않고 API Adapter에서 명시적으로 변환한다.

```text
프론트 intent: experience
AI mode: experience_extraction
```

---

## 7. AI Provider 관리 원칙

현재 기본 Provider는 Gemini다.

```env
AI_PROVIDER=gemini
```

기존 GPT 경로는 삭제하지 않는다. 나중에 다음 설정으로 전환할 수 있어야 한다.

```env
AI_PROVIDER=openai
```

모델의 일반 기본값은 코드에 둘 수 있고 `.env` 값으로 재정의할 수 있다.

```text
.env에 모델명이 있음
  → .env 값 사용

.env에 모델명이 없음
  → 코드 기본값 사용
```

비밀값은 로컬 `.env`에만 저장한다.

```env
GEMINI_API_KEY=
OPENAI_API_KEY=
```

`.env.example`에는 다른 PC에서 필요한 변수 이름과 안전한 기본값을 기록하되 실제 키는 기록하지 않는다.

---

## 8. 테스트를 이해하고 실행하는 방법

테스트는 하나가 아니라 다음 세 단계로 구분한다.

### 8.1 단위 테스트

실제 API 키 없이 코드 내부 규칙을 검사한다.

- Pydantic 스키마 검증
- 프론트엔드 필드 변환
- AI 구조화 출력 변환
- 오류 처리
- RAG 검색 및 인덱스 동기화
- Gemini/OpenAI Provider 선택

실제 AI 대신 미리 정한 응답을 반환하는 가짜 객체를 사용할 수 있다.

단위 테스트 통과는 실제 Gemini와 웹 연결이 완료됐다는 뜻이 아니다.

### 8.2 실제 AI 연동 테스트

로컬 `.env`의 API 키를 사용해 AI 엔진과 Gemini 또는 OpenAI 사이의 실제 통신을 검사한다.

- 모델 인증
- 모델 이름의 유효성
- 실제 응답 생성
- 구조화 출력
- 실제 임베딩 생성

이 테스트는 API 비용과 외부 서비스 상태의 영향을 받을 수 있으므로 단위 테스트와 분리한다.

### 8.3 웹 통합 테스트

다음 전체 흐름을 브라우저에서 확인한다.

```text
프론트엔드
  → 백엔드 API
  → AI 엔진
  → 실제 AI Provider
  → 백엔드 응답
  → 프론트엔드 화면
```

브라우저 확인 항목:

- 사용자의 메시지가 표시되는가
- 실제 AI 답변이 표시되는가
- 로딩 상태가 정상인가
- 오류 발생 시 입력 내용이 보존되는가
- 새로고침 후 대화가 복구되는가
- Mock 모드와 실제 API 모드가 올바르게 구분되는가

Codex는 작업 완료 시 어떤 단계의 테스트를 실행했는지 명확히 알려야 한다.

---

## 9. 기능 구현 순서

AI 엔진과 웹 연결은 다음 순서로 작은 단위로 진행한다.

1. 최소 FastAPI 서버 실행
2. 서버 상태 확인 API
3. 대화 생성 API
4. 비스트리밍 일반 채팅 메시지 API
5. 프론트엔드용 실제 HTTP API 구현
6. Mock/실제 HTTP 모드 전환
7. ChatbotAI와 Gemini 실제 연결
8. 브라우저에서 일반 텍스트 대화 확인
9. 오류 응답과 재시도 처리
10. SSE 스트리밍
11. 첨부파일 업로드와 텍스트 추출
12. 경험정리 AI 연결
13. 채용공고 분석 AI 연결
14. 확정 경험 RAG 연결
15. 전체 통합 테스트

처음부터 모든 API를 한꺼번에 만들지 않는다. 일반 텍스트 대화처럼 가장 작은 사용자 흐름을 실제로 완성한 뒤 다음 기능으로 확장한다.

---

## 10. 각 작업이 끝난 뒤 설명할 내용

Codex는 구현 완료 후 다음 내용을 사용자에게 알려야 한다.

1. 이번에 구현한 기능
2. 수정하거나 생성한 파일
3. 주요 데이터 흐름
4. 사용자가 직접 읽어볼 핵심 함수
5. 실행한 테스트와 결과
6. 브라우저 또는 터미널에서 직접 확인하는 방법
7. 아직 구현하지 않은 기능
8. 다음에 진행할 가장 작은 작업

설명할 때는 테스트 통과와 실제 웹 연동 성공을 구분한다.

```text
단위 테스트 통과
≠
실제 Gemini 웹 연동 완료
```

---

## 11. 현재 프로젝트에서 주의할 점

현재 채팅 화면은 `src/api/v2ChatApi.js`의 메모리 Mock 구현을 직접 사용한다.

따라서 `VITE_USE_MOCK=false`만 설정해도 채팅이 자동으로 Python 백엔드에 연결되지는 않는다.

또한 현재 다음 파일에는 실제 백엔드 조립 코드가 아직 없다.

- `AI_Engine/router.py`
- `AI_Engine/AI_langchain.py`

다음 구현에서는 기존 Mock 동작을 보존하면서 실제 HTTP 구현을 별도로 연결하고, 설정에 따라 두 구현 중 하나가 선택되도록 해야 한다.

프론트엔드의 `intent` 값과 AI 엔진의 `ChatMode` 값도 서로 다르므로 백엔드 Adapter에서 변환해야 한다.

---

## 12. 새로운 Codex 세션에 전달할 시작 요청

다른 PC나 새로운 세션에서는 다음과 같이 요청할 수 있다.

```text
먼저 AI_ENGINE_CODING_COLLABORATION_GUIDE.md를 끝까지 읽고,
문서에 적힌 작업 방식과 주석 규칙을 지켜서 진행해줘.

관련 서비스 흐름과 API 계약 문서도 확인한 뒤,
현재 작업 상태를 점검하고 다음으로 구현할 가장 작은 단계를 알려줘.

내가 설명이나 검토만 요청하지 않는 한 직접 구현하되,
학습 가능한 수준으로 코드를 나누고 각 코드의 역할과 이유를
한국어 주석으로 설명해줘.

기존 GPT 코드는 유지하고 Gemini를 기본 Provider로 사용해줘.
```

이 문서의 규칙과 사용자의 최신 요청이 충돌하면 사용자의 최신 요청을 우선한다.

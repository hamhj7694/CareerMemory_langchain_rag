# Career Memory 인증·계정 복구 설계

## 1. 인증 원칙

- 신규 사용자는 고유한 아이디를 만들고 아이디로 로그인한다.
- 인증 기능 도입 전 기존 계정은 아이디 설정 전까지 이메일 로그인을 지원한다.
- 비밀번호 원문은 저장하지 않고 Argon2id 해시만 저장한다.
- 로그인 세션은 예측할 수 없는 임의 토큰으로 만들고 DB에는 토큰의 SHA-256 해시만 저장한다.
- 브라우저에는 세션 토큰을 `HttpOnly`, `SameSite=Lax` 쿠키로 전달한다.
- 배포 환경의 쿠키에는 `Secure`를 적용하고 전체 서비스를 HTTPS로 제공한다.
- 프론트엔드의 화면 숨김과 관계없이 모든 데이터 소유권은 FastAPI에서 검사한다.

## 2. 사용자 흐름

### 회원가입

1. 이름, 고유 아이디, 이메일, 6자 이상의 비밀번호, 복구 질문과 답변을 입력한다.
2. FastAPI가 이메일 형식과 비밀번호 길이를 검증한다.
3. 비밀번호를 Argon2id로 해시해 `users`에 저장한다.
4. 로그인 세션을 발급하고 서비스 화면으로 이동한다.

초기 개발 단계에서는 이메일 인증 없이 가입할 수 있다.

인증 기능 도입 전에 생성된 로컬 대화는 개발 환경에서 첫 번째로 가입한 계정에 한 번 귀속한다. 운영 환경에서는 무소유 데이터를 자동 귀속하지 않는다.

### 로그인·로그아웃

1. 로그인 성공 시 임의 세션 토큰을 발급한다.
2. 토큰 원문은 HttpOnly 쿠키, 해시는 `auth_sessions`에 저장한다.
3. 로그아웃하면 서버 세션을 폐기하고 쿠키를 삭제한다.
4. 로그인 실패 메시지는 이메일 존재 여부와 비밀번호 오류를 구분하지 않는다.

### 아이디 찾기

회원가입에 사용한 이메일을 입력하면 등록된 아이디를 화면에 표시한다. 이 방식은 이메일 주소를 아는 사람이 아이디와 가입 여부를 확인할 수 있으므로 운영 전 IP·이메일 기준 요청 횟수 제한을 적용해야 한다.

### 비밀번호 분실

1. 사용자가 아이디, 이메일, 가입 시 선택한 질문, 복구 답변과 새 비밀번호를 입력한다.
2. 복구 답변은 정규화한 뒤 Argon2id 해시로 비교한다.
3. 질문과 답변이 맞으면 기존 비밀번호를 보여주지 않고 새 비밀번호 해시로 교체한다.
4. 변경 후 기존 로그인 세션을 모두 폐기하고 다시 로그인하게 한다.
5. 복구 확인을 5번 실패하면 해당 계정의 복구를 15분간 잠근다.

보안 질문은 이메일 링크나 복구 코드보다 추측 위험이 높다. 답변에 공개적으로 알려진 정보 사용을 피하도록 안내하고, 운영 서비스에서는 rate limit과 추가 인증 수단 도입을 검토한다.

## 3. 데이터 소유권

```text
users
  ├─ auth_sessions
  └─ conversations
       └─ messages
```

모든 대화 조회·수정·삭제·메시지 전송은 현재 로그인한 사용자의 `user_id` 조건을 함께 사용한다. 다른 사용자의 ID를 직접 입력한 경우에도 존재 여부를 구분하지 않고 `404`를 반환한다.

## 4. API

| Method | 경로 | 역할 |
|---|---|---|
| POST | `/api/v2/auth/register` | 회원가입 및 세션 발급 |
| POST | `/api/v2/auth/login` | 로그인 |
| POST | `/api/v2/auth/logout` | 현재 세션 폐기 |
| GET | `/api/v2/auth/me` | 현재 로그인 사용자 조회 |
| PUT | `/api/v2/auth/username` | 기존 사용자의 아이디 설정·변경 |
| POST | `/api/v2/auth/username/find` | 이메일로 아이디 조회 |
| PUT | `/api/v2/auth/recovery-question` | 로그인 사용자의 복구 질문 설정·변경 |
| POST | `/api/v2/auth/password/recover` | 아이디·이메일·복구 답변 확인 후 비밀번호 변경 |

## 5. 배포 전 추가 조건

- PostgreSQL 연결과 스키마 마이그레이션 도구 도입
- 로그인·복구 API의 IP·계정 기준 rate limit
- 복구 질문보다 강한 복구 코드 또는 MFA 도입 검토
- 운영 HTTPS와 `Secure` 쿠키 확인
- 개인정보 처리방침, 보존 기간, 계정 삭제 기능

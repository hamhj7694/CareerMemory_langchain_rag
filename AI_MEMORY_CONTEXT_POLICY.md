# AI 기억·대화 문맥 정책

## 1. 목적

Career Memory의 챗봇이 어떤 정보를 대화 세션 안에서 기억하고, 어떤 정보를 다른 대화에서도 공통으로 활용하는지 정의한다.

## 2. 기억 계층

```text
계정 공통 정보
├─ 사용자 이름
└─ 사용자가 확정하여 경험 관리에 저장한 경험
   └─ 모든 대화 세션에서 사용자별 RAG 검색 가능

대화 세션별 정보
├─ 현재 대화의 사용자·AI 메시지
├─ 현재 대화에 첨부한 파일
└─ 현재 대화의 미확정 경험 제안
   └─ 해당 대화 세션에서만 문맥으로 사용
```

### 장기 기억

사용자가 검토하고 확정하여 **경험 관리**에 저장한 경험을 장기 기억으로 사용한다.

- 로그인한 사용자의 모든 대화에서 검색할 수 있다.
- 질문과 관련된 경험만 RAG로 검색하여 AI에 전달한다.
- 모든 경험을 매 요청마다 전부 전달하지 않는다.
- 경험이 생성·수정·삭제되면 검색 인덱스를 함께 갱신한다.
- 답변에서 참고한 경험 ID와 근거를 추적할 수 있어야 한다.

### 단기 기억

현재 `conversation_id`에 속한 메시지와 첨부만 단기 기억으로 사용한다.

- 새 대화를 만들면 이전 대화 원문을 자동으로 가져오지 않는다.
- 서로 다른 대화 세션의 메시지를 하나의 프롬프트 문맥으로 합치지 않는다.
- 사용자가 다른 대화를 명시적으로 선택해 가져오는 기능은 추후 별도 기능으로 구현할 수 있다.

## 3. 저장 경계

챗봇 대화에서 경험으로 보이는 내용이 발견되더라도 AI가 곧바로 장기 기억으로 확정하지 않는다.

```text
현재 대화에서 경험 후보 발견
  → ExperienceDraft 또는 Proposal 생성
  → 사용자 미리보기·수정
  → 사용자 승인
  → 경험 관리 DB에 확정 저장
  → 사용자별 경험 RAG 인덱스 갱신
  → 이후 모든 대화에서 검색 가능
```

대화 세션을 삭제해도 이미 승인되어 경험 관리에 저장된 경험은 함께 삭제하지 않는다. 경험 삭제는 경험 관리에서 별도로 수행한다.

## 4. 챗봇 요청 문맥 조립

```text
로그인 사용자 확인
  → 현재 conversation_id의 소유권 확인
  → 현재 대화 메시지 복원
  → 사용자 질문으로 확정 경험 RAG 검색
     └─ user_id 필터 필수
  → 현재 대화 문맥 + 관련 경험 + 사용자 이름을 AI에 전달
  → 답변과 참고 경험 정보 반환
```

AI에 전달할 수 있는 정보:

- 현재 사용자의 표시 이름
- 현재 대화 세션의 메시지와 허용된 첨부 본문
- 현재 사용자의 확정 경험 중 질문과 관련된 검색 결과
- 각 경험에 연결된 근거와 인용 정보

기본적으로 전달하지 않는 정보:

- 다른 사용자의 모든 데이터
- 다른 대화 세션의 메시지 원문
- 사용자가 승인하지 않은 다른 세션의 경험 초안
- 질문과 무관한 전체 경험 데이터
- 이메일, 비밀번호 해시, 복구 답변 등 인증 정보

## 5. 보안과 검색 규칙

1. 관계형 DB 조회와 벡터 검색 모두 `user_id` 조건을 강제한다.
2. 프론트엔드가 전달한 `user_id`를 신뢰하지 않고 로그인 세션에서 사용자 ID를 얻는다.
3. 검색 결과의 각 문서는 `experience_id`, `user_id`, `index_version`을 가진다.
4. 검색 후에도 반환된 경험의 소유권을 다시 확인한다.
5. 사용자 A의 질문에서 사용자 B의 경험이 검색되지 않는 통합 테스트를 작성한다.
6. 답변이 검색 경험을 사용했다면 참고 경험을 사용자에게 확인 가능한 형태로 표시한다.

## 6. 구현 완료 조건

- 경험 CRUD가 백엔드 DB에 사용자별로 영구 저장된다.
- 확정 경험 생성·수정·삭제 이벤트가 RAG 인덱스와 동기화된다.
- 챗봇이 현재 세션 문맥과 계정 공통 경험을 함께 사용할 수 있다.
- 새 대화에서는 이전 대화 원문 없이도 저장 경험을 활용할 수 있다.
- 다른 대화의 미저장 내용은 새 대화에서 보이지 않는다.
- 계정 간 DB 접근 및 벡터 검색 격리 테스트가 통과한다.

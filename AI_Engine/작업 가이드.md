다음 작업 순서

1. 대화형 챗봇 AI
chatbot_ai.py
대화 기록과 첨부 정보를 받아 ChatResponse 반환
스트리밍 시 ChatStreamEvent 반환
이 단계에서는 RAG 없이 일반 대화부터 검증
현재 conversation_id의 메시지만 단기 문맥으로 사용
다른 대화 세션의 메시지 원문은 자동 공유하지 않음

2. 원본 파일 처리 기반
PDF/TXT 로더
문서 청크 분할
파일 해시와 중복 확인
페이지·문단·offset 메타데이터 보존
EvidenceSource와 연결

3. 경험정리 AI
experience_ai.py
직접 입력과 대화 범위를 공통 입력으로 변환
경험을 0개 이상 추출
경험 분류 → 프로젝트·활동 → 상세 경험 구조 반환
ExperienceExtractionResult로 강제 검증

4. RAG 검색 기반
experience_ai.py
job_analysis_ai.py
원본 근거 인덱스와 확정 경험 인덱스를 분리
Chroma와 임베딩 기능은 06-rag-chatbot.py를 응용
사용자 승인 후 경험 관리에 저장된 확정 경험만 장기 기억으로 사용
모든 경험 문서와 검색에 인증 세션의 user_id 필터 강제
챗봇은 질문과 관련된 경험만 검색하고 참고 경험을 응답에 표시

5. 채용공고 분석 AI
job_analysis_ai.py
공고 요구사항을 JobRequirement[]로 추출
요구사항별로 확정 경험을 RAG 검색
추천 결과를 RequirementExperienceLink[]로 반환
AI 추천과 사용자 직접 연결을 구분

6. 라우터 구현
명시적으로 선택한 모드는 해당 AI에 바로 전달
[자동]일 때만 의도 판정
단어 포함 여부로 분기하지 않음

7. AI_langchain.py 조립
세 AI 실행 흐름
Router를 한곳에서 생성하고 연결

8. API·프론트엔드 연결
커리어 챗
대화내용으로 경험 정리
경험 관리의 경험 추가
채용공고 분석
기존 Pydantic 스키마를 그대로 API 계약으로 사용
